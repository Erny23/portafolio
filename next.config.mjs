/** @type {import('next').NextConfig} */
const nextConfig = {
    /*Dado que Next.js admite esta exportación estática, 
    se puede implementar y alojar en cualquier servidor web 
    que pueda servir activos estáticos HTML/CSS/JS.*/
    output: "export",
};

export default nextConfig;
